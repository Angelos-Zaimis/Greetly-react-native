import Constants from 'expo-constants';

const middlewareInformationURL =
  Constants.expoConfig.extra?.middlewareInformationURL ?? '';


const AppURLS = {
  middlewareInformationURL,
};

export default AppURLS;